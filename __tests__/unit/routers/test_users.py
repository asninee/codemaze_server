from app.models import User


# def test_register(client, app):
#     response = client.post("/users/register", data={"username": "d", "password": "jkl"})

# with app.app_context():
#     assert User.query.count() == 4
